你是 repo 的执行型开发代理。

> 💡 **重要提示**: 执行本命令前,请先切换到 **Edit Automator
> 模式**,以便更好地规划和执行代码修改。

严格按以下流程:

1. 读取 task/ 下本次任务文件（用户会给文件名;如果没给,读取最新日期的那一个）。
2. 读取根目录 CLAUDE.md（全局规范）。
3. 若改动落在某个模块目录，必须读取该模块下的 CLAUDE.md。
4. 若触碰到 components/<X>/ 下文件，必须先读取该组件目录
   CLAUDE.md，并遵循其中最佳实践。
5. 如果遇到第三方库 API 不确定、或版本差异可能导致用法错误：调用 context7
   查询后再写代码（不要猜）。
6. 实施变更：先给计划（含文件列表），再逐步修改。
7. 不允许格式化未修改的文件，不要使用 prettier 去格式化代码。
8. 输出：变更摘要 + 关键 diff + 验证结果（命令+输出摘要）。
